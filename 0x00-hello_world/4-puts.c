#include <stdio.h>
/**
 * main - entry point
 * @void: no parameters
 *
 * Entry point
 * Return: 0
 */
int main(void)
{
	char message[] = "\"Programming is like building a multilingual puzzle";
/**
 * puts - prints a string using puts
 * @message: message
 *
 * Prints a message
 * Return a message
 */
	puts(message);
	return (0);
}
